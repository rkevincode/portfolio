import { assets, infoList, toolsData } from '@/src/assets/assets'
import Image from 'next/image'
import React from 'react'
import { motion } from "motion/react"

const About = ({isDarkMode}) => {
    return (
        <div>

        </div>
    )
}
  
export default About
  