import { assets, workData } from '@/src/assets/assets'
import Image from 'next/image'
import React from 'react'
import { motion } from "motion/react"

const Work = ({isDarkMode}) => {
    return (
        <div>

        </div>
    )
}
  
export default Work